spring:
  datasource:
    url: jdbc:h2:mem:ecommerce
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console

jwt:
  secret: ecommerce-dev-secret-key-that-is-at-least-256-bits-long!!
